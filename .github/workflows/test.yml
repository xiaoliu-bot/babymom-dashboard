name: Test Workflow

on:
  workflow_dispatch:
  repository_dispatch:
    types: [test]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: echo "Hello from test workflow"
      - name: Check git
        run: |
          pwd
          ls -la
          git status || echo "No git repo"
