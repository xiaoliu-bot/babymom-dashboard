/* ============================================================
 * 宝妈指数看板 — 配置
 * ------------------------------------------------------------
 * 页面数据来自 GitHub Action 每日生成的 data.json（同源读取，
 * 无跨域、无 token 泄露）。data.json 不存在时回退内置演示数据。
 * ============================================================ */

window.DASHBOARD_CONFIG = {
  // Action 生成的数据文件（部署后与页面同目录）
  dataFile: 'data.json',

  /* 刷新策略 */
  refresh: {
    manualButton: true,     // 顶部手动刷新按钮
    autoAfterClose: true,   // 每日 15:00 收盘后自动拉取一次
    closeHour: 15,          // 收盘小时（24h制）
    pollIntervalMs: 0,      // 0=不轮询；盯盘可设 30000(30s)/60000(1m)
  },

  mockFallback: true,   // data.json 拉取失败时用内置演示数据，保证页面始终能渲染
};
